# {agent_name} Agent

## Installation

Create a `custom.yaml` file in the agent folder:

```yaml
code: { agent_code }
name: '{agent_name}'
default_selected: true
```

Then run:

```bash
npx NETECMT-method install
```

Or if you have NETECMT-cli installed globally:

```bash
NETECMT install
```

## About This Agent

{agent_description}

_Generated with NETECMT Builder workflow_
